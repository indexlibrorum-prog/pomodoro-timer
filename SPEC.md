# あわポモ風タイマー 仕様書

最終更新: 2026-08-05 / 実装バージョン `2026-08-05 メモと計測タスクの分離`

`README.md` が「使い方」なのに対し、この文書は**動作の規則**を定める。
値や判定条件は `index.html` の実装と一致させること。

---

## 1. 位置づけ

期日と繰り返しを持つタスク管理に、ポモドーロタイマーと資格学習の復習リマインドを
統合した個人用アプリ。買い出しから資格勉強まで同じ場所で扱う。

### 設計の前提

| 前提 | 理由 |
| --- | --- |
| サーバーを持たない | 個人用。アカウント管理・同期の運用コストを負わない |
| データは端末内のみ | 外部送信は書誌検索のみ（後述） |
| 定期的な休憩を強制しない | 参考にした「あわわポモドーロ」の思想。急かすと続かない |
| 連続記録を煽らない | 同上。数値は出すが、途切れても何も起きない |

### 参考にした設計

- <https://note.com/awawa_adhd/n/n203716fd6e8b> 1分アイドリング、定期休憩なし
- <https://note.com/awawa_adhd/n/n13860e0392bd> タスク別の時間記録
- <https://note.com/noko_app/n/n0effd52ea016> 試験日からの逆算、ヒートマップ
- <https://note.com/eyemask57/n/n6e4c1c65469f> 今日タブ中心、やり残しを持ち越さない

---

## 2. 構成

単一ページアプリ。`index.html` に HTML / CSS / JS をすべて内包する。

| ファイル | 役割 |
| --- | --- |
| `index.html` | 本体。これ以外に実行時の依存はない（バーコード読み取りを除く） |
| `manifest.json` | PWA 定義。`display: standalone` |
| `sw.js` | Service Worker。ネットワーク優先、失敗時キャッシュ |
| `icon-180/192/512.png` | アイコン |
| `vendor/zxing-reader.js` + `zxing_reader.wasm` | バーコード読み取り（zxing-wasm 3.1.2 / MIT）。約 1.1MB |
| `serve.js` | 動作確認用のローカルサーバー。Node 標準ライブラリのみ |

配信は GitHub Pages（<https://indexlibrorum-prog.github.io/pomodoro-timer/>）。

> **URL を変えてはいけない。** 記録は localStorage にオリジン単位で保存されるため、
> URL が変わると過去のデータを読めなくなる。移設時は先にバックアップを書き出す。

---

## 3. データモデル

localStorage に 6 キーで保存する。すべて JSON。

| キー | 内容 |
| --- | --- |
| `awapomo.tree` | ノード（フォルダ／タスク）の連想配列 `{id: node}` |
| `awapomo.records` | 日別集計 `{'YYYY-MM-DD': {work, rest, sessions}}`。work/rest は秒 |
| `awapomo.log` | 計測ログ `[{start, sec, label, nodeId}]` 新しい順・最大 2000 件 |
| `awapomo.done` | 完了ログ `[{id, name, date, at}]` 新しい順・最大 1000 件 |
| `awapomo.cfg` | 設定 |
| `awapomo.ui` | 画面状態（選択中の対象、開いているタブなど） |

### 3.1 ノード

フォルダとタスクは同じ連想配列に入れ、`type` で区別する。親子は `parent` で表す
（ルートは `null`）。階層の深さに制限は設けないが、`pathOf()` は 20 段で打ち切る。

**共通**

| フィールド | 内容 |
| --- | --- |
| `id` | 一意キー。`Date.now()` の36進数 + 乱数5文字 |
| `type` | `'folder'` \| `'task'` |
| `name` | 表示名 |
| `parent` | 親フォルダの id。ルートは `null` |
| `createdAt` | 作成時刻（ms） |
| `order` | 同じ親・同じ type の中での並び順 |

**フォルダ固有**

| フィールド | 既定 | 内容 |
| --- | --- | --- |
| `color` | パレット順送り | 一覧の左端の帯、グラフの色 |
| `track` | `true` | **時間を測るか**（§5） |
| `study` | `false` | 資格・学習モード（§7） |
| `startDate` / `endDate` | `null` | 期間。学習モードでは `endDate` を**試験日**として扱う |
| `autoSteps` | `true` | 試験日から復習間隔を自動調整するか |
| `steps` | `''` | このフォルダの復習周期（カンマ区切り）。空なら設定の既定 |
| `isbn` / `author` / `publisher` / `cover` | — | 参考書として登録した場合のみ |

**タスク固有**

| フィールド | 既定 | 内容 |
| --- | --- | --- |
| `due` | `null` | 期日 `'YYYY-MM-DD'` |
| `dueTime` | `null` | 期日の時刻 `'HH:MM'`。任意。`due` が無ければ必ず `null` |
| `recur` | `{freq:'none', every:1, days:[], monthDay:null}` | 繰り返し（§6） |
| `planMin` | `0` | 予定時間（分）。0 は未設定 |
| `doneAt` | `null` | 最後に完了にした日 |
| `review` | `false` | 忘却曲線で復習するか（学習モードのフォルダ内でのみ有効） |
| `reviewIdx` | `0` | 復習の段階（周期配列の添字） |
| `nextReview` | `null` | 次回復習日 |
| `prev` | — | 完了の取り消し用に、直前の `doneAt`/`nextReview`/`reviewIdx`/`due` を控える |

### 3.2 設定（`awapomo.cfg`）

| キー | 既定 | 内容 |
| --- | --- | --- |
| `idlingMin` | `1` | まず何分だけやるか |
| `autoSec` | `15` | 選択画面で無操作のとき自動続行するまでの秒数 |
| `checkinMin` | `0` | 作業中の声かけ間隔（分）。0 で無効 |
| `steps` | `'1,3,7,14'` | 復習周期の既定（日） |
| `reviewHour` | `9` | `.ics` に書き出すときの既定時刻 |
| `trackRoot` | `false` | ホーム直下のタスクも時間を測るか |
| `noiseKind` | `'brown'` | `brown` \| `pink` \| `white` |
| `noiseVol` | `35` | 0–100 |
| `noiseAuto` | `false` | 作業開始でノイズを鳴らす |
| `noiseInRest` | `false` | 休憩中もノイズを続ける |
| `sound` | `true` | 通知音 |
| `notify` | `false` | デスクトップ通知 |
| `wake` | `false` | 画面を消灯させない（Screen Wake Lock） |
| `hideRec` | `false` | 学習機能（復習・参考書スキャン）を隠す |

### 3.3 後方互換

読み込み時にマイグレーションはしない。**参照時に旧形式を吸収する。**

| 旧 | 扱い |
| --- | --- |
| `repeat: 'daily' \| 'weekly' \| 'none'` | `recurOf()` が `recur` 相当に変換して返す |
| `track` 未定義のフォルダ | `true`（測る）扱い |
| `dueTime` 未定義 | 終日扱い |

---

## 4. 画面

下部タブで 4 画面。実行中のタイマーは、タイマータブ以外に小さいバーで常駐する。

| タブ | 役割 |
| --- | --- |
| **今日** | 遅れているもの／今日やること／期日なし。起点 |
| **タイマー** | 対象の選択、ポモドーロ、作業用ノイズ |
| **一覧** | フォルダ／タスクのツリー。追加・編集・カレンダー書き出し |
| **記録** | 集計、7日グラフ、カレンダー、当日ログ、設定 |

### 4.1 今日タブの分類

未完了のタスクを、**期日**と**次回復習日**のうち早い方（`left`）で振り分ける。

| 区分 | 条件 | 並び |
| --- | --- | --- |
| 遅れているもの | `left < 0` | 遅れが大きい順 → 時刻順 |
| 今日やること | `left === 0` | 終日が先 → 時刻の早い順 |
| 期日なし | 期日も復習日も無い | 折りたたみ。**今日やることが空なら自動で開く** |

**文言の規則**（達成していないのに祝わない）

| 状況 | 表示 |
| --- | --- |
| タスクが1件も無い | 「タスクがまだありません。…」 |
| 当日の完了が 1 件以上 | 「今日ぶんは片づきました。（N件 完了）」 |
| 当日の完了が 0 件 | 「今日が期日のタスクはありません。」 |

---

## 5. 時間を測るタスクと、測らないタスク

買い出しや家賃振込にタイマーの UI は不要。**フォルダ単位**で決め、タスク作成時には判断させない。

### 判定 `isTracked(id)`

1. 祖先にフォルダが 1 つも無い（＝ホーム直下）→ `cfg.trackRoot`（既定 `false`）
2. 祖先のフォルダに 1 つでも `track === false` があれば `false`
3. それ以外は `true`

学習モードを ON にしたフォルダは `track` を強制的に `true` にする（測る前提のため）。

### 「測らない」場合に止めるもの

| 対象 | 挙動 |
| --- | --- |
| 今日タブの ▶ ボタン | 出さない |
| タイマーの対象候補 | 出さない（**ただし現在の対象になっていれば残す**） |
| 登録時の自動対象設定 | しない |

期日・繰り返し・完了・カレンダー書き出しは**測る／測らないに関係なく動く**。
家賃振込の通知は従来どおり届く。

**例外**: タイマー画面の自由入力から「＋一覧へ」で登録した場合は、
測る意思があると見なして `track` に関わらず対象にする。

---

## 6. 繰り返し

### 種類

| `freq` | 追加パラメータ | 例 |
| --- | --- | --- |
| `daily` | `every`（n日ごと） | 毎日 / 3日ごと |
| `weekly` | `every`, `days`（0=日〜6=土、複数可） | 毎週 火・金 |
| `monthly` | `every`, `monthDay`（1–31 または `'last'`） | 毎月27日 / 毎月末 |
| `yearly` | — | 毎年 |

### 次回期日 `nextDue(n, from)`

`from`（既定は現在の `due`）**より後**で最初に該当する日を返し、
過去なら今日以降になるまで繰り返す（上限 500 回）。

- `monthly` は「基準日より後で最初に来る◯日」を最大13か月先まで探す。
  31日指定でその月に無ければ**その月の末日**に丸める
- 新規作成で期日が空なら、**昨日を基準**に探す。条件に合えば今日が最初の期日になる

### やり残しの扱い（自動リセット）

起動時と日付またぎに `autoReset()` を実行する。

| 種類 | 期日が過ぎている場合 |
| --- | --- |
| `daily` / `weekly` | **次の該当日へ送る**（遅延として残さない） |
| `monthly` / `yearly` | **そのまま残す**（家賃振込のように落とせないもの） |

### 完了

- 完了 → `doneAt = 今日`、`due = nextDue()`、完了ログに追加、`prev` に直前状態を控える
- 取り消し → `prev` から `doneAt` / `nextReview` / `reviewIdx` / `due` を**完全復元**し、完了ログから削除
- 完了表示の判定 `isDone(n)`
  - 復習タスク: `nextReview` が明日以降なら完了扱い
  - 繰り返しタスク: `doneAt === 今日` のときだけ完了扱い
  - 単発タスク: `doneAt` があれば完了

---

## 7. 学習モード

フォルダの「資格・学習モード」を ON にしたときだけ、配下のタスクに復習設定が出る。
`cfg.hideRec` が真なら機能ごと隠す。

### 試験日

学習モードのフォルダでは `endDate` を**試験日**として扱い、日付欄は 1 つになる。
一覧に「試験まで あとN日」を表示し、過ぎたフォルダは「終了」表示＋半透明にする。

### 復習間隔 `nextReviewOf(task)`

```
interval = 周期[min(reviewIdx, 周期.length-1)]
試験日があり autoSteps が真なら:
    残り日数 = 試験日 - 今日
    残り日数 <= 0 → null（もう出さない）
    interval = max(1, min(interval, floor(残り日数 / 2)))
次回復習日 = 今日 + interval
```

> 標準の忘却曲線で進めつつ、**試験までの残り日数の半分を超えない**。
> 試験が近づくほど自動的に間隔が詰まり、必ず試験前にもう一度復習が入る。

周期はフォルダごとに上書きでき、未指定なら `cfg.steps`（既定 1,3,7,14）を使う。

### 試験日を過ぎたら

`examPassed(id)` が真の復習タスクは、**今日タブ・タイマーの対象候補・`.ics` から外す**。
タスク自体は残るので、一覧から削除するか完了にする。

### 参考書のバーコード登録

学習モードのフォルダ内でのみ利用できる。

1. カメラで EAN-13 を読む（zxing-wasm）。`978`/`979` 始まりかつチェックディジットが
   正しいものだけ採用し、下段の価格コードを弾く
2. Google Books と openBD に**同時に問い合わせ**、取れた方を採用。書影は
   openBD → Google Books → 国立国会図書館サーチ の順で探す
3. 取得した書名でフォルダを作り、ISBN・著者・出版社・書影を保存する。
   書影は 96px に縮小して端末内に持つ（取得できなければ表紙なし）
4. 同じ ISBN は二重登録せず、既存を選択する

カメラは https もしくは `http://localhost` でのみ動作する。
使えない環境では ISBN 13 桁の手入力に切り替わる。

---

## 8. タイマー

### 状態遷移

```
IDLE ──開始──▶ IDLING ──時間切れ──▶ CHOICE ──続ける／自動──▶ WORK
                 │                      │                      │
                 │                      └──休憩する────────┐  │
                 ├──「もう乗った」──▶ WORK                  ▼  │
                 └──休憩する──────────────────────▶ REST ◀┘
                                                          │
                                        「作業にもどる」──┘
  いずれの状態からも「終了／やめる」で IDLE へ
```

| 状態 | 表示 | 説明 |
| --- | --- | --- |
| `IDLE` | じゅんび | 開始待ち |
| `IDLING` | アイドリング | `idlingMin` 分のカウントダウン。とりあえず手をつける |
| `CHOICE` | どうする？ | 続ける／休憩する。`autoSec` 秒の無操作で自動続行 |
| `WORK` | 作業ちゅう | **カウントアップ。定期的な休憩は入らない** |
| `REST` | きゅうけい | カウントアップするだけ。長さは管理しない |

`checkinMin > 0` のとき、作業中にその間隔で再び `CHOICE` を出す（既定は無効）。

### 時間の記録

- 経過時間は**時刻ベース**で計算する。バックグラウンドで JS が止まっても、
  復帰後の経過時間は正しい
- `IDLING` と `WORK` を作業時間、`REST` を休憩時間として日別集計に加算する。
  `CHOICE` の待ち時間は加算しない
- 10 秒ごと、および状態遷移・画面非表示・離脱時に集計へ反映する
- 復帰時に 6 時間を超える差分は異常値として捨てる
- **作業 30 秒未満のセッションはログに残さない**（誤操作を記録しない）

### 対象の選択

`ui.lockedId` が計測対象。タスクでもフォルダでも指定できる（フォルダ＝案件単位）。

対象候補は 4 区分で並べる。

| 区分 | 内容 |
| --- | --- |
| 今日のもの | 期日・復習日が今日以前。時刻の早い順 |
| そのほか | 残り。フォルダごとにまとまる |
| フォルダごとに測る | 全フォルダ（📁 付き） |
| 完了ずみ | 完了済みタスク（やり直し用） |

対象を決めずに測ることもでき、その場合のみ自由入力欄が出る。
入力した内容は「＋一覧へ」でタスクとして登録でき、**計測中でも**
「この作業を一覧に登録」で登録と同時に進行中の記録を紐づけられる。

### 作業用ノイズ

ブラウン／ピンク／ホワイトノイズを起動時に生成し、WAV にして `<audio>` でループする。

- 音源ファイルを持たないのでオフラインでも鳴る
- 20 秒生成し、末尾 0.5 秒を先頭へクロスフェードしてから切り落とし、継ぎ目を消す
- `<audio>` 要素を使うため、iOS のロック画面／コントロールセンターに再生操作が出る
- 自動再生の制限を避けるため、鳴らし始めは必ずボタン操作の流れで行う

---

## 9. 記録

| 指標 | 定義 |
| --- | --- |
| 今日の作業 | 当日の作業秒の合計 |
| 今日の回数 | 当日に記録されたセッション数 |
| 連続日数 | 作業 60 秒以上の日が続いた数。当日が未達なら前日から数える |

- **7日グラフ** — 「タスク別」で日ごとの積み上げ表示。階層 1〜3 で集計の粒度を変える。
  ノードに色があればその色、無ければパレット順。記録が無ければ説明を出す
- **カレンダー** — 月単位のヒートマップ。日付の下に分、下部に月合計と実施日数
- **ログ** — 当日の計測ログを最大 20 件

---

## 10. 通知

**PWA は端末内で通知を予約できない。** そのため予定をカレンダーに渡し、
iOS 標準の通知を鳴らす方式を取る。

一覧の「📅 カレンダーへ」で、現在の階層以下を `.ics` として書き出す。

| 種類 | 出力 |
| --- | --- |
| 繰り返しタスク | **RRULE 1 件**。一度入れれば以降ずっと通知される |
| 単発タスク | 期日が今日以降のもののみ |
| 復習 | 次回復習日。試験が終わっていれば出さない |
| 試験日 | 学習モードのフォルダの `endDate` |

- 時刻は `dueTime` があればその時刻、無ければ `cfg.reviewHour`（既定 9 時）
- ローカル時刻から UTC に変換して出力する（TZID を使わない）
- すべての予定に `VALARM`（開始時刻ちょうど）を付ける

> 復習日は消化するたびに変わるので、**ときどき書き出し直す**運用になる。
> 繰り返しタスクは入れ直し不要。

---

## 11. データの保全

- ホーム画面に追加した PWA は、Safari の「7日間使わなければ消す」対象外。
  使用日数のカウンタが独立し、使うたびにリセットされる
- ただし数週間まったく起動しないと iOS 側の判断で消える可能性は残る

そのため次を用意する。

- 設定 →「書き出す」で JSON を出力。iPhone では**共有シート**が開き、
  「ファイルに保存」で iCloud Drive に置ける
- 前回の書き出しから **2 週間**経ち、記録が 3 件以上あると画面上部に案内を出す
  （「あとで」で 3 日後に再表示）
- 「読み込む」で復元。確認の上、現在のデータを置き換える

---

## 12. 外部への通信

通常の利用ではどこにも通信しない。**参考書を登録するときだけ** ISBN を送信する。

| 送信先 | 用途 |
| --- | --- |
| `www.googleapis.com` | Google Books API（書誌・書影） |
| `api.openbd.jp` | openBD（書誌・書影） |
| `ndlsearch.ndl.go.jp` | 国立国会図書館サーチ（書影のみ） |

止めたい場合は `lookupIsbn()` が空を返すよう書き換えれば、ISBN と書名の手入力で運用できる。

---

## 13. 既知の制約

| 制約 | 内容 |
| --- | --- |
| バックグラウンド通知 | 裏に回している間はタイマーの音が鳴らない。経過時間の計算は正しい |
| 消音スイッチ | iOS で消音にしていると通知音もノイズも鳴らない |
| カメラ | https / localhost でのみ動作 |
| 書影 | 公開されていない書籍が多く、その場合は 📕 アイコンになる |
| 復習のカレンダー登録 | 日付が動くため入れ直しが必要 |
| 同期 | 端末間の同期は無い。移行は JSON の書き出し／読み込み |

---

## 14. 変更時の約束

- **URL（リポジトリ名）を変えない。** 変えると全記録が読めなくなる
- 既存データを壊さない。新フィールドは**未定義でも従来どおり動く**ようにする
- 設定のバージョン表示（`BUILD`）を更新する。どの版が動いているか確認できるようにするため
- 変更後は `README.md` の該当箇所も直す
