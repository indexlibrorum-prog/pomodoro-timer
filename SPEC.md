# あわポモ風タイマー 仕様書

最終更新: 2026-08-06 / 実装バージョン `2026-08-06 記録の信頼性まわりの修正`

`README.md` が「使い方」なのに対し、この文書は**動作の規則**を定める。
値や判定条件は `index.html` の実装と一致させること。

---

## 1. 位置づけ

期日と繰り返しを持つタスク管理に、ポモドーロタイマーと資格学習の復習リマインドを
統合した個人用アプリ。買い出しから資格勉強まで同じ場所で扱う。

### 設計の前提

| 前提 | 理由 |
| --- | --- |
| サーバーを持たない | 個人用。アカウント管理・同期の運用コストを負わない |
| データは端末内のみ | 外部送信は書誌検索のみ（後述） |
| 定期的な休憩を強制しない | 参考にした「あわわポモドーロ」の思想。急かすと続かない |
| 連続記録を煽らない | 同上。数値は出すが、途切れても何も起きない |

### 参考にした設計

- <https://note.com/awawa_adhd/n/n203716fd6e8b> 1分アイドリング、定期休憩なし
- <https://note.com/awawa_adhd/n/n13860e0392bd> タスク別の時間記録
- <https://note.com/noko_app/n/n0effd52ea016> 試験日からの逆算、ヒートマップ
- <https://note.com/eyemask57/n/n6e4c1c65469f> 今日タブ中心、やり残しを持ち越さない

---

## 2. 構成

単一ページアプリ。`index.html` に HTML / CSS / JS をすべて内包する。

| ファイル | 役割 |
| --- | --- |
| `index.html` | 本体。これ以外に実行時の依存はない（バーコード読み取りを除く） |
| `manifest.json` | PWA 定義。`display: standalone` |
| `sw.js` | Service Worker。ネットワーク優先、失敗時キャッシュ |
| `icon-180/192/512.png` | アイコン |
| `vendor/zxing-reader.js` + `zxing_reader.wasm` | バーコード読み取り（zxing-wasm 3.1.2 / MIT）。約 1.1MB |
| `serve.js` | 動作確認用のローカルサーバー。Node 標準ライブラリのみ |

配信は GitHub Pages（<https://indexlibrorum-prog.github.io/pomodoro-timer/>）。

> **URL を変えてはいけない。** 記録は localStorage にオリジン単位で保存されるため、
> URL が変わると過去のデータを読めなくなる。移設時は先にバックアップを書き出す。

---

## 3. データモデル

localStorage に 8 キーで保存する。すべて JSON。

| キー | 内容 |
| --- | --- |
| `awapomo.tree` | ノード（フォルダ／タスク）の連想配列 `{id: node}` |
| `awapomo.records` | 日別集計 `{'YYYY-MM-DD': {work, rest, sessions}}`。work/rest は秒 |
| `awapomo.log` | 計測ログ `[{start, sec, label, nodeId}]` 新しい順・最大 2000 件 |
| `awapomo.done` | 完了ログ `[{id, name, date, at}]` 新しい順・最大 1000 件 |
| `awapomo.cfg` | 設定 |
| `awapomo.ui` | 画面状態（選択中の対象、開いているタブなど） |
| `awapomo.timer` | **実行中のタイマー**（§8.3）。`IDLE` のときは削除される |
| `awapomo.autobackup` | バックアップ読み込みの**直前に退避**した現在データ |

### 3.1 ノード

フォルダとタスクは同じ連想配列に入れ、`type` で区別する。親子は `parent` で表す
（ルートは `null`）。**階層は 20 段までとし、それ以上の追加はフォームで止める**
（`pathOf()` の打ち切りと一致させている）。

**ノードを移動する機能は無い**ため、循環参照は通常の操作では発生しない
（壊れたバックアップの読み込みに備えて、検証時にのみ確認する）。

**共通**

| フィールド | 内容 |
| --- | --- |
| `id` | 一意キー。`Date.now()` の36進数 + 乱数5文字 |
| `type` | `'folder'` \| `'task'` |
| `name` | 表示名 |
| `parent` | 親フォルダの id。ルートは `null` |
| `createdAt` | 作成時刻（ms） |
| `order` | 同じ親・同じ type の中での並び順 |

**フォルダ固有**

| フィールド | 既定 | 内容 |
| --- | --- | --- |
| `color` | パレット順送り | 一覧の左端の帯、グラフの色 |
| `track` | `true` | **時間を測るか**（§5） |
| `study` | `false` | 資格・学習モード（§7） |
| `startDate` / `endDate` | `null` | 期間。学習モードでは `endDate` を**試験日**として扱う |
| `autoSteps` | `true` | 試験日から復習間隔を自動調整するか |
| `steps` | `''` | このフォルダの復習周期（カンマ区切り）。空なら設定の既定 |
| `isbn` / `author` / `publisher` / `cover` | — | 参考書として登録した場合のみ |

**タスク固有**

| フィールド | 既定 | 内容 |
| --- | --- | --- |
| `due` | `null` | 期日 `'YYYY-MM-DD'` |
| `dueTime` | `null` | 期日の時刻 `'HH:MM'`。任意。`due` が無ければ必ず `null` |
| `recur` | `{freq:'none', every:1, days:[], monthDay:null}` | 繰り返し（§6） |
| `planMin` | `0` | 予定時間（分）。0 は未設定 |
| `doneAt` | `null` | 最後に完了にした日 |
| `review` | `false` | 忘却曲線で復習するか（学習モードのフォルダ内でのみ有効） |
| `reviewIdx` | `0` | 復習の段階（周期配列の添字） |
| `nextReview` | `null` | 次回復習日 |
| `prev` | — | 完了の取り消し用に、直前の `doneAt`/`nextReview`/`reviewIdx`/`due` を控える |

### 3.2 設定（`awapomo.cfg`）

| キー | 既定 | 内容 |
| --- | --- | --- |
| `idlingMin` | `1` | まず何分だけやるか |
| `autoSec` | `15` | 選択画面で無操作のとき自動続行するまでの秒数 |
| `checkinMin` | `0` | 作業中の声かけ間隔（分）。0 で無効 |
| `steps` | `'1,3,7,14'` | 復習周期の既定（日） |
| `reviewHour` | `9` | `.ics` に書き出すときの既定時刻 |
| `trackRoot` | `false` | ホーム直下のタスクも時間を測るか |
| `noiseKind` | `'brown'` | メイン音。ノイズ3種または環境音5種 |
| `noiseVol` | `35` | 0–100 |
| `noiseMixOn` | `false` | 2つ目の音を重ねる |
| `noiseMixKind` | `'rain'` | 重ねる音の種類 |
| `noiseMixVol` | `20` | 重ねる音の音量（0–100） |
| `noiseAuto` | `false` | 作業開始でノイズを鳴らす |
| `noiseInRest` | `false` | 休憩中もノイズを続ける |
| `sound` | `true` | 通知音 |
| `notify` | `false` | デスクトップ通知 |
| `wake` | `false` | 画面を消灯させない（Screen Wake Lock） |
| `hideRec` | `false` | 学習機能（復習・参考書スキャン）を隠す |
| `googleClientId` | `''` | Google Calendar API用のWeb OAuth Client ID |

### 3.3 後方互換

読み込み時にマイグレーションはしない。**参照時に旧形式を吸収する。**

| 旧 | 扱い |
| --- | --- |
| `repeat: 'daily' \| 'weekly' \| 'none'` | `recurOf()` が `recur` 相当に変換して返す |
| `track` 未定義のフォルダ | `true`（測る）扱い |
| `dueTime` 未定義 | 終日扱い |

---

## 4. 画面

下部タブで 4 画面。実行中のタイマーは、タイマータブ以外に小さいバーで常駐する。

| タブ | 役割 |
| --- | --- |
| **今日** | 遅れているもの／今日やること／期日なし。起点 |
| **タイマー** | 対象の選択、ポモドーロ、作業用ノイズ |
| **一覧** | フォルダ／タスクのツリー。追加・編集・カレンダー書き出し |
| **記録** | 集計、7日グラフ、カレンダー、当日ログ、設定 |

### 4.1 今日タブの分類

未完了のタスクを、**期日**と**次回復習日**のうち早い方（`left`）で振り分ける。

| 区分 | 条件 | 並び |
| --- | --- | --- |
| 遅れているもの | `left < 0` | 遅れが大きい順 → 時刻順 |
| 今日やること | `left === 0` | 終日が先 → 時刻の早い順 |
| 期日なし | 期日も復習日も無い | 折りたたみ。**今日やることが空なら自動で開く** |

**文言の規則**（達成していないのに祝わない）

| 状況 | 表示 |
| --- | --- |
| タスクが1件も無い | 「タスクがまだありません。…」 |
| 当日の完了が 1 件以上 | 「今日ぶんは片づきました。（N件 完了）」 |
| 当日の完了が 0 件 | 「今日が期日のタスクはありません。」 |

---

## 5. 時間を測るタスクと、測らないタスク

買い出しや家賃振込にタイマーの UI は不要。**フォルダ単位**で決め、タスク作成時には判断させない。

### 判定 `isTracked(id)`

1. 祖先にフォルダが 1 つも無い（＝ホーム直下）→ `cfg.trackRoot`（既定 `false`）
2. 祖先のフォルダに 1 つでも `track === false` があれば `false`
3. それ以外は `true`

### 学習モードとの関係（優先順位）

**祖先の `track === false` が常に優先される。** 階層継承の原則を崩さないため、
矛盾する配置は UI で作れないようにする。

- `track === false` のフォルダの配下では、**学習モードを有効化できない**
  （チェックボックスを無効化し、理由を表示する）
- 学習モードを ON にしたフォルダは、**自分自身の** `track` が `true` になる
  （祖先を上書きするわけではない）
- 既存データで矛盾（`track=false` の配下に `study=true`）が見つかった場合、
  `isTracked()` は `false` を返し、**編集画面に警告を表示**する。復習リマインドは働かない
- `track` を OFF にしようとしたフォルダの配下に学習モードがある場合は確認を出す

### 「測らない」場合に止めるもの

| 対象 | 挙動 |
| --- | --- |
| 今日タブの ▶ ボタン | 出さない |
| タイマーの対象候補 | 出さない（**ただし現在の対象になっていれば残す**） |
| 登録時の自動対象設定 | しない |

期日・繰り返し・完了・カレンダー書き出しは**測る／測らないに関係なく動く**。
家賃振込の通知は従来どおり届く。

**例外**: タイマー画面の自由入力から「＋一覧へ」で登録した場合は、
測る意思があると見なして `track` に関わらず対象にする。

---

## 6. 繰り返し

### 種類

| `freq` | 追加パラメータ | 例 |
| --- | --- | --- |
| `daily` | `every`（n日ごと） | 毎日 / 3日ごと |
| `weekly` | `every`, `days`（0=日〜6=土、複数可） | 毎週 火・金 |
| `monthly` | `every`, `monthDay`（1–31 または `'last'`） | 毎月27日 / 毎月末 |
| `yearly` | — | 毎年 |

### 次回期日 `nextDue(n, from)`

`from`（既定は現在の `due`）**より後**で最初に該当する日を返し、
過去なら今日以降になるまで繰り返す（上限 500 回）。

- `monthly` は「基準日より後で最初に来る◯日」を最大13か月先まで探す。
  31日指定でその月に無ければ**その月の末日**に丸める
- `yearly` も同じく月の日数で丸める。**2月29日は非うるう年では2月28日**になる
  （3月1日に流れない）
- 新規作成で期日が空なら、**昨日を基準**に探す。条件に合えば今日が最初の期日になる

### 端数・異常値の扱い

| 条件 | 挙動 |
| --- | --- |
| `every` が 0 / 負 / 未定義 | `1` に丸める |
| `weekly.days` が空 | 基準日と同じ曜日を使う |
| `monthly.monthDay` が未定義 | 基準日の「日」を使う |
| `monthly.monthDay=31` で31日が無い月 | その月の末日 |
| 500 回で今日以降に到達しない | `null` を返す。**呼び出し側は期日を変えない**（繰り返しを進めない） |

### やり残しの扱い（自動リセット）

起動時と日付またぎに `autoReset()` を実行する。

| 種類 | 期日が過ぎている場合 |
| --- | --- |
| `daily` / `weekly` | **次の該当日へ送る**（遅延として残さない） |
| `monthly` / `yearly` | **そのまま残す**（家賃振込のように落とせないもの） |

### 完了

- 完了 → `doneAt = 今日`、`due = nextDue()`、完了ログに追加、`prev` に直前状態を控える
- 取り消し → `prev` から `doneAt` / `nextReview` / `reviewIdx` / `due` を**完全復元**し、完了ログから削除
- 完了表示の判定 `isDone(n)`
  - 復習タスク: `nextReview` が明日以降なら完了扱い
  - 繰り返しタスク: `doneAt === 今日` のときだけ完了扱い
  - 単発タスク: `doneAt` があれば完了

---

## 7. 学習モード

フォルダの「資格・学習モード」を ON にしたときだけ、配下のタスクに復習設定が出る。
`cfg.hideRec` が真なら機能ごと隠す。

### 継承の範囲（関数ごとに異なるので注意）

| 判定 | 範囲 |
| --- | --- |
| `studyOn(id)` | **祖先のいずれか**が `study` なら真 |
| `examOf(id)` | **最も近い**祖先の試験日 |
| `stepsFor(id)` | **最も近い**祖先の周期。無ければ `cfg.steps` |
| `autoOf(id)` | **最も近い**祖先の `autoSteps` |

学習フォルダの下に参考書フォルダを作る使い方を想定しているため、
設定値は最も近い祖先から取り、有効・無効の判定だけは祖先全体を見る。

試験日や周期を変更しても、**既存の `nextReview` は再計算しない**。
次に完了チェックを入れた時点から新しい設定が効く。

### 試験日

学習モードのフォルダでは `endDate` を**試験日**として扱い、日付欄は 1 つになる。
一覧に「試験まで あとN日」を表示し、過ぎたフォルダは「終了」表示＋半透明にする。

### 復習間隔 `nextReviewOf(task)`

```
interval = 周期[min(reviewIdx, 周期.length-1)]      ← 現在の reviewIdx で引く
試験日があり autoSteps が真なら:
    残り日数 = 試験日 - 今日
    残り日数 <= 0 → null（もう出さない）
    interval = max(1, min(interval, floor(残り日数 / 2)))
次回復習日 = 今日 + interval
```

> 標準の忘却曲線で進めつつ、**試験までの残り日数の半分を超えない**。
> 試験が近づくほど自動的に間隔が詰まり、試験日までにもう一度復習機会が入る。

**`reviewIdx` の適用順**: 次回日を**現在の `reviewIdx` で決めてから**、`reviewIdx` を 1 進める。
したがって周期 `1,3,7,14` のタスクは、**初回完了の次回が 1 日後**、その次が 3 日後になる。

**残り日数が 1 日のとき**は `floor(1/2)=0` → `max(1, 0)=1` となり、
**次回復習日は試験当日**になる。試験前日に必ず入るわけではない。

周期はフォルダごとに上書きでき、未指定なら `cfg.steps`（既定 1,3,7,14）を使う。

### 試験日を過ぎたら

`examPassed(id)` が真の復習タスクは、**今日タブ・タイマーの対象候補・`.ics` から外す**。
タスク自体は残るので、一覧から削除するか完了にする。

### 参考書のバーコード登録

学習モードのフォルダ内でのみ利用できる。

1. カメラで EAN-13 を読む（zxing-wasm）。`978`/`979` 始まりかつチェックディジットが
   正しいものだけ採用し、下段の価格コードを弾く
2. Google Books と openBD に**同時に問い合わせ**、取れた方を採用。書影は
   openBD → Google Books → 国立国会図書館サーチ の順で探す
3. 取得した書名でフォルダを作り、ISBN・著者・出版社・書影を保存する。
   書影は 96px に縮小して端末内に持つ（取得できなければ表紙なし）
4. 同じ ISBN は二重登録せず、既存を選択する

カメラは https もしくは `http://localhost` でのみ動作する。
使えない環境では ISBN 13 桁の手入力に切り替わる。

---

## 8. タイマー

### 状態遷移

```
IDLE ──開始──▶ IDLING ──時間切れ──▶ CHOICE ──続ける／自動──▶ WORK
                 │                      │                      │
                 │                      └──休憩する────────┐  │
                 ├──「もう乗った」──▶ WORK                  ▼  │
                 └──休憩する──────────────────────▶ REST ◀┘
                                                          │
                                        「作業にもどる」──┘
  いずれの状態からも「終了／やめる」で IDLE へ
```

| 状態 | 表示 | 説明 |
| --- | --- | --- |
| `IDLE` | じゅんび | 開始待ち |
| `IDLING` | アイドリング | `idlingMin` 分のカウントダウン。とりあえず手をつける |
| `CHOICE` | どうする？ | 続ける／休憩する。`autoSec` 秒の無操作で自動続行 |
| `WORK` | 作業ちゅう | **カウントアップ。定期的な休憩は入らない** |
| `REST` | きゅうけい | カウントアップするだけ。長さは管理しない |

`checkinMin > 0` のとき、作業中にその間隔で再び `CHOICE` を出す（既定は無効）。

### セッションの境界

**`IDLE` から始まり `IDLE` に戻るまでが 1 セッション**。
途中で `WORK` と `REST` を何度往復しても 1 回と数える。

- 30 秒未満の判定は **`IDLING` + `WORK` の合計**（`st.sessionWork`）で行う
- 回数（`records.sessions`）とログは、**セッションを開始した日**に付ける
- 手動の「終了／やめる」後は 10 秒間だけ取り消せる。取り消すと終了で追加した回数とログを戻し、
  終了時点までの作業時間を保ったまま元の状態へ復帰する。取り消し操作までの時間は計上しない
- 6 時間超の中断による自動打ち切りは取り消し対象にしない

### 時間の記録

- 経過時間は**時刻ベース**で計算する。バックグラウンドで JS が止まっても、
  復帰後の経過時間は正しい
- `IDLING` と `WORK` を作業時間、`REST` を休憩時間として日別集計に加算する。
  `CHOICE` の待ち時間は加算しない
- 10 秒ごと、および状態遷移・画面非表示・離脱時に集計へ反映する
- **加算はローカル暦日の境界で分割する**（`addSpan()`）。23:50 に始めて 00:10 に復帰した
  20 分は、前日と当日に 10 分ずつ入る。時間だけが分かれ、回数とログは開始日のまま
- 中断（前回の反映からの差分）が **6 時間**を超えたら異常値として捨てる。
  区間の確定（`endSeg()`）にも同じ上限を掛ける

**作業 30 秒未満のセッションはログに残さない**（誤操作を記録しない）。
終了時には記録に残らなかったことを画面上で明示する。
日別集計には反映済みの時間が残るため、ログの合計と日別集計は完全には一致しない。

### 実行状態の保存と復元（`awapomo.timer`）

再読込やタブの復帰でセッションを失わないよう、状態そのものを保存する。

| フィールド | 内容 |
| --- | --- |
| `mode` | `IDLING` / `CHOICE` / `WORK` / `REST`（`IDLE` ならキーごと削除） |
| `segStart` | 現在の区間の開始時刻 |
| `sessionStart` | セッション開始時刻 |
| `sessionWork` / `sessionRest` | 確定済みの秒 |
| `label` / `nodeId` | 表示名と計測対象 |
| `choiceStart` | 選択画面に入った時刻 |
| `counted` | 回数を計上済みか |
| `lastFlush` | 最後に集計へ反映した時刻 |

保存は状態遷移時・10 秒ごと・`visibilitychange` / `pagehide` / `beforeunload`。

**復元の規則**

| 中断時間（`now - lastFlush`） | 挙動 |
| --- | --- |
| 6 時間以内 | そのまま復元して継続。中断中の時間も作業時間として数える |
| 6 時間超 | **セッションを打ち切る**。確定済みの分だけログに残して `IDLE` へ戻す。中断分は計上しない |

OS による強制終了で最後の 10 秒ぶんが未反映になることは許容する。

### 対象の選択

`ui.lockedId` が計測対象。タスクでもフォルダでも指定できる（フォルダ＝案件単位）。

対象候補は 4 区分で並べる。

| 区分 | 内容 |
| --- | --- |
| 今日のもの | 期日・復習日が今日以前。時刻の早い順 |
| そのほか | 残り。フォルダごとにまとまる |
| フォルダごとに測る | 全フォルダ（📁 付き） |
| 完了ずみ | 完了済みタスク（やり直し用） |

対象を決めずに測ることもでき、その場合のみ自由入力欄が出る。
入力した内容は「＋一覧へ」でタスクとして登録でき、**計測中でも**
「この作業を一覧に登録」で登録と同時に進行中の記録を紐づけられる。

計測中は対象名をタイマー上部の強調枠に固定表示する。登録済みの対象はフォルダ階層を含む名前と色、
自由入力は入力名を表示する。フェーズ、対象、ノイズ操作には読み上げ用の状態・ラベルを付ける。

### 作業用ノイズ

ブラウン／ピンク／ホワイトノイズと、雨／換気扇・空調／波／電車／焚き火から選択する。
2つ目の音を有効にすると、別の `<audio>` 要素で同時再生し、それぞれ独立した音量を持つ。

- ノイズ3種は起動時に20秒生成し、末尾0.5秒を先頭へクロスフェードして継ぎ目を消す
- 環境音5種は24 kHz / mono / 16-bit / 24秒の同梱WAV。2秒のクロスフェードでループをつなぐ
- 同梱WAVは `tools/generate-ambient-audio.js` から決定的に再生成でき、外部素材を使わない
- すべてService Workerで事前キャッシュし、オフラインでも鳴る
- `<audio>` 要素を使うため、iOS のロック画面／コントロールセンターに再生操作が出る
- 自動再生の制限を避けるため、鳴らし始めは必ずボタン操作の流れで行う

### Googleカレンダー

- Google Identity Servicesのトークンモデルを使い、`calendar.readonly` だけを要求する
- アクセストークンはメモリ内だけに保持し、localStorageやバックアップへ保存しない
- primaryカレンダーの過去7日〜未来90日を取得し、「今日」画面には当日の予定だけ表示する
- 予定キャッシュは `awapomo.gcal` に保存し、オフライン時も前回取得分を表示する
- 切断時はトークンをrevokeし、端末内の予定キャッシュを削除する
- 今日の予定は「タイマーへ」で予定名を自由入力の計測対象に設定し、タイマー画面へ移動できる
- 予定の選択だけでは計測を開始せず、計測中は別の予定への切り替えを無効化する

---

## 9. 記録

| 指標 | 定義 |
| --- | --- |
| 今日の作業 | 当日の作業秒の合計 |
| 今日の回数 | 当日に記録されたセッション数 |
| 連続日数 | 作業 60 秒以上の日が続いた数。当日が未達なら前日から数える |

- **7日グラフ** — 「タスク別」で日ごとの積み上げ表示。階層 1〜3 で集計の粒度を変える。
  ノードに色があればその色、無ければパレット順。記録が無ければ説明を出す
- **カレンダー** — 月単位のヒートマップ。日付の下に分、下部に月合計と実施日数
- **ログ** — 当日の計測ログを最大 20 件

---

## 10. 通知

**PWA は端末内で通知を予約できない。** そのため予定をカレンダーに渡し、
iOS 標準の通知を鳴らす方式を取る。

一覧の「📅 カレンダーへ」で、現在の階層以下を `.ics` として書き出す。

| 種類 | 出力 |
| --- | --- |
| 繰り返しタスク | **RRULE 1 件**。一度入れれば以降ずっと通知される |
| 単発タスク | 期日が今日以降のもののみ |
| 復習 | 次回復習日。試験が終わっていれば出さない |
| 試験日 | 学習モードのフォルダの `endDate` |

- 時刻は `dueTime` があればその時刻、無ければ `cfg.reviewHour`（既定 9 時）
- ローカル時刻から UTC に変換して出力する（TZID を使わない）
- すべての予定に `VALARM`（開始時刻ちょうど）を付ける

### UID

RFC 5545 が求める永続的・一意な `UID` を、**同じ予定には常に同じ値**で出力する。

```
単発・繰り返し   UID:task-{node.id}@awapomo
試験             UID:exam-{folder.id}@awapomo
復習             UID:review-{task.id}@awapomo
```

**復習の UID に日付を含めない。** 含めると復習日が動くたびに別イベントになり、
書き出し直すたびにカレンダーへ積み上がってしまう。

> **同一 UID を出力するが、再インポート時に既存予定を更新するか重複させるかは
> カレンダーアプリ側の挙動に依存する。** 重複した場合は手動で整理する必要がある。

復習日は消化するたびに変わるので、**ときどき書き出し直す**運用になる。
繰り返しタスクは RRULE 1 件で入るため入れ直し不要。

---

## 11. データの保全

- ホーム画面に追加した PWA は、Safari の「7日間使わなければ消す」対象外。
  使用日数のカウンタが独立し、使うたびにリセットされる
- ただし数週間まったく起動しないと iOS 側の判断で消える可能性は残る

また、ブラウザの保存は既定では best-effort であり、容量不足や端末の判断で消える可能性がある。
起動時に `navigator.storage.persist()` を best-effort で要求する（失敗しても続行）。

### 書き出し

```json
{
  "schemaVersion": 1,
  "appBuild": "2026-08-06 ...",
  "exportedAt": "2026-08-06T05:00:00.000Z",
  "data": { "tree": {}, "records": {}, "logs": [], "dones": [], "cfg": {}, "ui": {} }
}
```

iPhone では**共有シート**が開き、「ファイルに保存」で iCloud Drive に置ける。
前回の書き出しから **2 週間**経ち、記録が 3 件以上あると画面上部に案内を出す
（「あとで」で 3 日後に再表示）。

### 読み込み

**1 つでも失敗したら一切変更しない。** 順に検証する。

1. JSON として解釈できるか
2. 形式の判定（`data` があれば新形式、`v` があれば旧形式のフラット構造）
3. `schemaVersion` が既知か（未来のバージョンは拒否）
4. 型（`tree`/`records`/`cfg`/`ui` はオブジェクト、`logs`/`dones` は配列）
5. ノードの整合性（`id` がキーと一致、`type` が `folder`/`task`、親が存在するか `null`）
6. 循環参照が無いか
7. 現在のデータを `awapomo.autobackup` へ退避
8. すべて成功したら一括で置き換え、再読込

失敗時は**理由を表示して中止**する。旧形式（`v:2` のフラット構造）も読める。

### 保存の失敗

`localStorage.setItem()` は必ず `try...catch` で囲み、**失敗を握りつぶさない**。

- 失敗したら画面上部に消えない警告を出す（容量不足の可能性を伝える）
- **失敗している間は「最終◯◯に書き出し済み」と表示しない**

---

## 12. 外部への通信

通常の利用ではどこにも通信しない。**参考書を登録するときだけ** ISBN を送信する。

| 送信先 | 用途 |
| --- | --- |
| `www.googleapis.com` | Google Books API（書誌・書影） |
| `api.openbd.jp` | openBD（書誌・書影） |
| `ndlsearch.ndl.go.jp` | 国立国会図書館サーチ（書影のみ） |

止めたい場合は `lookupIsbn()` が空を返すよう書き換えれば、ISBN と書名の手入力で運用できる。

---

## 13. ツリーの操作

| 操作 | 挙動 |
| --- | --- |
| フォルダの削除 | **配下を再帰的に削除**する。確認を出す |
| 削除後の記録 | 計測ログ・完了ログは**残る**。参照先を失った記録はグラフで「未分類」に集計される |
| 並び替え | 同じ親・同じ種別の中でのみ、↑↓ で入れ替える |
| 移動（親の変更） | **機能として無い**。循環参照は通常の操作では発生しない |
| 階層 | 20 段まで。それ以上の追加は止める |
| 計測対象の削除 | `ui.lockedId` は `null` に戻る |

---

## 14. 既知の制約

| 制約 | 内容 |
| --- | --- |
| バックグラウンド通知 | 裏に回している間はタイマーの音が鳴らない。経過時間の計算は正しい |
| 消音スイッチ | iOS で消音にしていると通知音もノイズも鳴らない |
| カメラ | https / localhost でのみ動作 |
| 書影 | 公開されていない書籍が多く、その場合は 📕 アイコンになる |
| 復習のカレンダー登録 | 日付が動くため入れ直しが必要 |
| 同期 | 端末間の同期は無い。移行は JSON の書き出し／読み込み |
| 強制終了 | OS に落とされると、最後の 10 秒ぶんの作業時間が未反映になりうる |
| ログと集計 | 30 秒未満のセッションは集計に入るがログには残らないため、両者は完全一致しない |

---

## 15. 日付・時刻の共通規則

| 規則 | 内容 |
| --- | --- |
| 基準 | すべて**端末のローカル暦日**で扱う |
| 日付の文字列 | `YYYY-MM-DD`。**UTC として解釈しない**（`new Date('2026-08-05')` を使わず、年月日から組み立てる） |
| 「今日」 | ローカル時刻の 0 時から 24 時 |
| 日数差 | ミリ秒差ではなく**暦日差**で求める（`diffDays()`） |
| 時刻 | `HH:MM`。`.ics` 出力時だけローカル時刻から UTC に変換する |
| 日付またぎ | 作業・休憩時間は暦日の境界で分割して加算する |
| 日付の変化 | 1 分ごとに監視し、日が変わったら今日タブ・繰り返し・集計を組み直す |
| タイムゾーン変更 | 保存済みの `YYYY-MM-DD` は変換しない。移動後の新しいローカル日で判定する |
| 端末時刻の変更 | 過去に戻すと、その間の記録は新しい日付に付く。**検出も補正もしない** |

---

## 16. データ整合性

| 事象 | 扱い |
| --- | --- |
| ID の重複 | 連想配列のキーなので構造上発生しない |
| 存在しない親 | 通常の操作では発生しない。読み込み時に検証して弾く |
| 循環参照 | 移動機能が無いため発生しない。読み込み時に検証して弾く |
| 不正な型 | 読み込み時に検証して弾く |
| 設定値の範囲 | 入力欄の `min`/`max` で丸める。`steps` は正の整数のみ採用し、空なら既定値 |
| 未知のフィールド | 無視して保持する（前方互換） |
| 保存の失敗 | 警告を表示し、以降「保存済み」と表示しない |
| 複数タブの同時編集 | **後から保存した方が勝つ**。タブ間の同期はしない（個人用のため許容） |

---

## 17. エラー時の表示

| 事象 | 表示 | 続行 |
| --- | --- | --- |
| カメラの許可が下りない | 「カメラを開けませんでした。ISBN を手入力してください」 | ISBN 手入力へ切替 |
| 読み取りエンジンを読めない | 「読み取りエンジンを読み込めませんでした」 | 同上 |
| 書誌が見つからない | 「書誌情報が見つかりませんでした。書名を入力して登録できます」 | 手入力で登録可 |
| 書影が取得できない | 表示しない（📕 アイコン） | する |
| 通知の許可が下りない | 設定のスイッチを自動で戻す | する |
| Wake Lock が使えない | 何も出さない | する |
| 保存容量が不足 | 画面上部に消えない警告 | するが記録は残らない |
| バックアップが壊れている | 理由を明示して中止。**データは変更しない** | する |
| `.ics` に出す予定が無い | 「カレンダーに入れる予定がありません」 | する |

---

## 18. 対応環境

| 区分 | 環境 |
| --- | --- |
| 主対象 | iPhone のホーム画面に追加した Web アプリ（iOS 16.4 以降） |
| 副対象 | iOS Safari、PC 版 Chrome |
| 動作保証外 | プライベートブラウズ、古い iOS、`file://` からの直接起動 |

- カメラ（`getUserMedia`）と Service Worker は**安全なコンテキスト**が必要。
  https もしくは `http://localhost` でのみ動作する
- Screen Wake Lock は Safari 18.4 以降。非対応環境では黙って無効
- `file://` では localStorage は使えるが、PWA・カメラ・wasm の読み込みができない

---

## 19. 受入テスト

| # | 項目 | 期待 |
| --- | --- | --- |
| 1 | 毎週火・金を水曜に作成 | 最初の期日が金曜 |
| 2 | 31日指定を2月に進める | その月の末日 |
| 3 | 毎年2月29日を非うるう年へ | 2月28日（3月1日にしない） |
| 4 | 日次タスクを3日放置 | 今日へリセット |
| 5 | 月次タスクを3日放置 | 遅延のまま残る |
| 6 | 試験前日に復習完了 | 次回は試験当日 |
| 7 | 日付をまたいでタイマー実行 | 前日と当日に分かれて集計 |
| 8 | 作業29秒で終了 | ログに残らない |
| 9 | 作業30秒で終了 | ログに残る |
| 10 | 実行中に再読込 | セッションが復元される |
| 11 | 実行中に7時間放置して復帰 | セッションが打ち切られ、確定分だけログに残る |
| 12 | フォルダを `track=false` へ変更 | 配下の ▶ と対象候補が消える |
| 13 | `track=false` の下で学習モードを ON | できない（理由を表示） |
| 14 | 壊れた JSON を読み込み | 一切変更されず、理由が出る |
| 15 | `{"foo":1}` を読み込み | 同上 |
| 16 | 親が存在しない／循環するデータ | 同上 |
| 17 | 旧形式（`v:2`）を読み込み | 正常に復元される |
| 18 | 同じ `.ics` を2回インポート | 復習が重複しない（UID が同じ） |
| 19 | 21階層目にフォルダ追加 | 止められる |
| 20 | `track` 未定義の旧データ | 従来どおり「測る」扱い |

---

## 20. 変更時の約束

- **URL（リポジトリ名）を変えない。** 変えると全記録が読めなくなる
- 既存データを壊さない。新フィールドは**未定義でも従来どおり動く**ようにする
- `localStorage.setItem()` は必ず `try...catch` で囲み、失敗を握りつぶさない
- `.ics` の UID を変えない。変えると既存の予定が重複する
- データ形式を変えたら `schemaVersion` を上げ、**旧形式も読めるようにする**
- 設定のバージョン表示（`BUILD`）を更新する。どの版が動いているか確認できるようにするため
- 変更後は `README.md` と本書の該当箇所、§19 の受入テストも直す
